# Checkpoint P17 — 2026-05-08T00:00:00Z

## Phase row
| P17 | Off-chain perimeter — frontend taint analyzer + scope artifact + RPC fingerprinter | `plan-feature-architecture` → `code-with-tests-first` → `codex` | P10 | D | 140 calls | 220k |

## Inputs read
- `/root/Silica/ops/perimeter-playbook.md:1-80` — Surfaces 1, 3 + authorization workflow

## Artifacts produced
- `src/scope/artifact.ts` — 50 lines (ScopeArtifact Zod schema)
- `src/scope/store.ts` — 30 lines (port + InMemoryScopeStore)
- `src/scope/enforce.ts` — 65 lines (requireScope fail-closed gate)
- `tools/perimeter/frontend/run.py` — 100 lines (frontend taint analyzer)
- `tools/perimeter/rpc/run.py` — 90 lines (RPC fingerprinter)
- `tests/unit/scope/enforce.test.ts` — 7 tests
- `tools/perimeter/frontend/tests/test_run.py` — 6 tests
- `tools/perimeter/rpc/tests/test_run.py` — 4 tests

## Commands run
```bash
npx vitest run --pool=forks --fileParallelism=false tests/unit/scope/
# 7 passed
python3 -m pytest tools/perimeter/frontend/tests/test_run.py  # 6 passed
python3 -m pytest tools/perimeter/rpc/tests/test_run.py       # 4 passed
npm run typecheck  # exit 0
```

## Gates passed
- C (TDD): 17 tests pass across scope enforcement + frontend + RPC
- Scope enforcement fail-closed: both Python tools raise ScopeMissing without scope_artifact_id
- BadgerDAO synthetic injection detection: confirmed in mock mode (test_detects_badgerdao_pattern)
- D (drift gate): validated

## Findings
None.

## Notes / surprises
- scope/artifact.ts imports only Zod — domain-pure, no infrastructure imports. PASS.
- Frontend tool has a mock mode (SILICA_MOCK_TOOLS env) for CI without Docker.
- ScopeMissing is raised before any network calls — correct fail-closed behavior.

## Verdict
complete
