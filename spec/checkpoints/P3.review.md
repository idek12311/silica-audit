# Review of P3 — 2026-05-08T00:00:00Z

## Layer 1 — Requirement fidelity (bob:873-883)

Phase row: Heuristic schema TS + Postgres migrations + three-pool storage per design/heuristic-schema.md and notes.md §17.4.

Deliverables:
- `src/heuristic/schema.ts`: HeuristicSchema with id, version, lineage, status (proposed/active/deprecated), deprecated, name, summary, category, vm_scope[], taxonomy_links, confidence_prior, fp_rate_observed, tp_rate_observed, n_observations, severity_default, tenant_visibility, tenant_id, minted_by, regression_cases, implementations, false_positive_shapes, applicability — matches heuristic-schema.md.
- aggregateHeuristicConfidence: formula P = 1 − ∏(1 − h_i.confidence_prior × h_i.weight) from Contract D.
- `src/heuristic/store.ts`: HeuristicStore interface (port) + InMemoryHeuristicStore (test double). queryForTenant correctly implements three-pool visibility.
- `src/heuristic/lifecycle.ts`: proposed→active→deprecated transitions; promoteToActive guard.
- Migrations: 0001_heuristic.sql with vm_scope GIN index, tenant_visibility index; 0002_finding.sql; 0003_audit.sql; 0004_audit_event.sql matching 10-reporting-contract.md §H schema exactly.

Spec invariant #5 (three-pool heuristic isolation): queryForTenant shows public + shared + own-tenant private only. Test confirms other tenant's private heuristics are not visible.

Finding: None. **Layer 1: PASS**

## Layer 2 — Logic & edge cases (bob:873-883)

- Duplicate create throws — tested.
- All versions of a heuristic by stable ID — tested.
- promoteToActive requires implementation + regression_case — both tested separately.
- Terminal state (deprecated → active) is rejected — tested.
- Three-pool: public visible to all, shared visible to all, private only to own tenant — all 4 combinations tested.

Finding: None. **Layer 2: PASS**

## Layer 3 — API integrity (bob:873-883)

- Zod schema field types match heuristic-schema.md §Schema verbatim.
- tenant_visibility enum ('public','shared-pool','private-tenant') matches SQL CHECK constraint.
- 0004_audit_event.sql table schema matches 10-reporting-contract.md §H exactly: id ULID TEXT, audit_id REFERENCES audit(id), ts TIMESTAMPTZ, state TEXT, agent_id TEXT, router TEXT, finding_id REFERENCES finding(id), cost_usd NUMERIC(12,4), tokens_in INTEGER, tokens_out INTEGER, details JSONB.

Finding: None. **Layer 3: PASS**

## Layer 4 — Security (bob:873-883)

- HeuristicStore is a port interface owned by the domain layer — no Postgres client imports in heuristic/*.ts. PASS (Invariant #1).
- Tenant isolation: private heuristics filtered at query boundary. No leakage path through queryForTenant.
- No credentials in code.

Finding: None. **Layer 4: PASS**

## Layer 5 — Context awareness (bob:873-883)

- store.ts does NOT import pg or ioredis — port interface is owned by the domain. PASS.
- Heuristic schema imports from Zod only.
- Migration files reference tables defined in prior migrations (0004 REFERENCES 0003 audit, 0004 REFERENCES 0002 finding) — correct dependency order.

Finding: None. **Layer 5: PASS**

## Layer 6 — Test quality (bob:873-883)

- 28 tests across 3 files.
- Behavioral assertions: Zod validation, lifecycle state machine transitions, three-pool query isolation.
- The three-pool isolation test is the most critical behavioral assertion — it proves Invariant #5 is enforced at the data layer.
- No xfail/skip.

Finding: None. **Layer 6: PASS**

## Summary

| Layer | Status | Findings |
|-------|--------|----------|
| L1 | PASS | 0 |
| L2 | PASS | 0 |
| L3 | PASS | 0 |
| L4 | PASS | 0 |
| L5 | PASS | 0 |
| L6 | PASS | 0 |

## Verdict
verdict: approve
