# Spec author-review — 2026-05-08T20:51:37Z

Forked-reviewer six-layer audit of the Silica v1 executor spec at `/root/Silica/spec/`. Read-only stance, opposing objective per bob:884-889 — finding what is wrong, not validating what is right. Spec is the 12-markdown set (README + 00..10) plus tests/test_spec_structure.py and the empty checkpoints/ + decisions/ directories. 21 phases (P0–P20). 4 parallel groups (A–D). Acceptance §A–§O.

## Layer 1 — Requirement fidelity

- 03-phase-map.md:7-29 — observation: All three v1 axes are covered. EVM front spans P1–P13 (spine schemas + EVM tools + bench cases + heuristics). SVM front spans P14–P16. Off-chain perimeter spans P17–P18. Final acceptance gate (P20) walks §A–§O. 21 phases match notes.md:502-565 §17.1 v1 axes commitment. — severity low (no defect; coverage complete)
- 02-acceptance-criteria.md:43, 36-43 — observation: §C claims to cover all 8 schema stressors as a single integration test (`bench/stressors/`) but the acceptance text only asserts an opaque "all 8 passing" exit code; no per-stressor visibility into which stressors actually round-tripped. With stressor 7 known to expose a v0.1 schema gap, a single bash-exit gate is brittle if a future stressor is silently skipped during refactor. — severity low
- 02-acceptance-criteria.md:166-177, README.md:117 — observation: §N grep checks 5 CLAUDE.md sections (Architecture posture / Dependency rules / TDD policy / Verification gates / Hard stops), but the README at line 117 ("CLAUDE.md authored with required sections") and write-agent-rules/SKILL.md:26-40 require 9 sections. §N as written does not enforce 4 of those 9 (Project shape / Naming rules / Review checklist / Debt policy). Acceptance is weaker than the locked artifact contract. — severity medium
- 03-phase-map.md:24, 01-use-case-frame.md:21 — observation: P15 names 5 SVM specialist agents (CPI-authority, missing-PDA, account-cosplay, sysvar-spoofing, arbitrary-CPI), but multi-vm-svm-sketch.md:55-100 enumerates 7 SVM-specific bug classes (adds SVM-MISSING-OWNER-CHECK and SVM-DUPLICATE-ACCOUNT-MUTABLE). Spec reduces SVM scope from 7 → 5 without explicit non-goal entry. The `01-use-case-frame.md:208-217` non-goals list does NOT call this out. — severity medium

## Layer 2 — Logic & edge cases

- 03-phase-map.md:11-29 — observation: Phase Map DAG resolves topologically (`test_phase_table_dependencies_resolve` confirmed). No cycles. No orphans. Group A (P1, P2, P3) all depend on P0 only — independent. Group B (P4, P5, P6) intra-dependencies (P4→P1, P5→P1, P6→P1+P2) preclude starting B until A complete; the spec calls B "independent" but it is "B is independent within itself, dependent on A" — text is correct but worth noting B cannot start in parallel with A. — severity low
- 04-parallelization-plan.md:131-175, 03-phase-map.md:21-22 — observation: Group C parallelizes P12 (5 EVM bench cases) and P13 (30 EVM heuristics). Worker C2 writes `scripts/seed-heuristics.ts` and `heuristics/baseline/evm/HEUR-*.json` which gets persisted to the heuristic Postgres table at `status=Active`. Worker C1 writes `bench/cases/evm/*/expected-finding.json` — and §I acceptance requires "at least one finding has cited a heuristic" via `jsonb_array_length(heuristics_cited) > 0`. C1's expected-finding.json must reference heuristic IDs that C2 is concurrently creating. **Shared-mutable-state risk via the heuristic naming convention** — C1 cannot verify C2's HEUR-* IDs exist when authoring expected-finding.json. The spec marks C1 + C2 as independent but they share the heuristic-ID namespace. — severity medium
- 03-phase-map.md:12, 28; 04-parallelization-plan.md:55-69, 161-175 — observation: P3 (worker A3) and P19 both claim "three-pool storage" responsibility. P3 worker A3 writes `src/heuristic/store.ts (CRUD + three-pool query)` as a flat file. P19's test uses `src/heuristic/{store,mining,drift}/` (subdirectory layout). Per §7 phase-data-immutability, P3's store.ts is frozen for P19; P19 must either extend or wrap, but the file-layout shape conflicts (flat vs subdir). Spec does not specify which is canonical. — severity medium
- 07-verification-gates.md:136-150, 03-phase-map.md:21-29 — observation: Per-phase gate row for P11 in §H shows "C (RED then GREEN against bench:evm euler), F (Euler-finding lessons), B (subset)". F is "Behavioral coverage audit (P20 sub-gate)" — the table earlier (§F) explicitly scopes F as a P20 sub-gate. Listing F at P11 contradicts §F's own scoping; either F runs at every phase touching tests (and the §F section is wrong) or it only runs at P20 (and the §H table is wrong). — severity medium
- 08-failure-recovery.md:5-21 — observation: Retry table covers tools/timeouts/rate-limits/Postgres/etc. **Reviewer `block` is 0 retries — terminal**. But §3 of 09-stop-conditions.md describes "advisor escalation triggers" and §1 lists hard stops including reviewer block. The transition from "block" → "halt + STOP-<id>.md" → "user resolves" → "re-attempt phase from scratch" (per 09-stop-conditions.md §5 step 3) means block is **not actually 0 retries** in practice — it is 0 immediate retries with manual restart possible. The retry table conflates "automated retry count" with "terminal" misleadingly. — severity low
- 08-failure-recovery.md:87-97 — observation: "Phase-level retry budget" says "at most one full re-attempt before escalation" — but the retry table (line 14) for `proceed-with-changes` says "Apply if ≤3 findings" with no retry count column. If a reviewer returns proceed-with-changes 3 times in succession (each round adds findings), the spec is silent on whether each cycle counts as a retry. Two-Correction Rule (bob:867-870) is referenced inconsistently — applied at phase granularity in §87-97 but at correction granularity in line 15. — severity low
- 09-stop-conditions.md:5-19 — observation: Hard stops list looks complete for sensitive paths (CLAUDE.md, skills, notes/design/research/ops, sister projects, secrets), but does not include "Spec self-validator (`validate_spec.py`) regression caused by mid-execution edits to spec text". 07-verification-gates.md §D drift gate handles citation drift; 09 §1 mentions validator failure ("Validator (`validate_spec.py --strict`) exits non-zero on the spec"); the two are aligned but the "loss-of-integrity" definition does not include "an executor accidentally edits spec/*.md content during execution" beyond drift — a hard stop on any spec/*.md mutation by anything other than ADR is implied but not explicit. — severity low

## Layer 3 — API integrity

- 00-identity.md:23 — observation: Cites `/root/Silica/notes.md:138` for "Audit-job state machine"; line 138 is "Cross-contract complexity (modern DeFi spans 20+ contracts)" inside §3.10 ("Hard parts (where projects die)"). No audit-job state-machine content at notes.md:138. The state-machine content lives in this spec at 01-use-case-frame.md:174-191 (Contract E); no notes.md anchor exists. **Citation is fabricated/wrong.** — severity high
- 00-identity.md:24 — observation: Cites `/root/Silica/notes.md:178` for "Five runtime routers"; line 178 is `https://www.kali.org/tools/hexstrike-ai/` inside §5 (HexStrike sources). The 5 routers are at notes.md:221-227 (§6) and notes.md:477. **Citation is wrong.** — severity high
- 00-identity.md:25 — observation: Cites `/root/Silica/notes.md:330` for "LLM gateway with caching + trust tiers"; line 330 is "**CREATE2 / counterfactual**: contracts that don't exist yet but will at known addresses" inside §11.1 (proxy patterns). Trust-tier content is at notes.md:536-544 (§17.3). **Citation is wrong.** — severity high
- 00-identity.md:26 — observation: Cites `/root/Silica/notes.md:170` for "Three baseline agents"; line 170 is a blank line (§4 ends at 169). The 3 agents (Analyzer / Prover / Skeptic) are not enumerated by name at any single notes.md line — they appear in passing in the routers/spine sections. **Citation is wrong.** — severity high
- 00-identity.md:34 — observation: Cites `/root/Silica/notes.md:217` for "Repo agent rules (architecture, TDD, naming, debt)"; line 217 is "A tool doesn't flip between modes at runtime. Slither always sandboxed; source flattener always in-process. Decided once at registration." inside §6 (MCP vs Direct). No CLAUDE.md / agent-rules content at line 217. **Citation is wrong.** — severity high
- 02-acceptance-criteria.md:141, 07-verification-gates.md:126 — observation: Both files cite `notes.md:415` for "5 prompt-injection patterns"; line 415 is a blank line within §12.2 ("Mainnet-fork state realism"). The actual prompt-injection content is at notes.md:364-373 (§11.7 "Prompt injection from contract source"), and it shows **only 3 example patterns** (// @notice IGNORE, // @custom:security-claim, ATTACKER_NOTE string constant). The spec claims "5 canonical injection patterns" enumerating "BadgerDAO-style comment, NatSpec-stuffing, identifier-stuffing, custom_security_claim string, file-import malicious code" — **2 of those (identifier-stuffing, file-import-malicious-code) are not in the source dossier** at all. Citation is wrong AND the count "5" is fabricated. — severity high
- 03-phase-map.md:34 — observation: Cites `notes.md:227-275` for "repo conventions" (P0.review). Lines 227-275 cover §6 router table closing + §7 four-primitive spine + §8 five-extension-surfaces — not "repo conventions". The actual CLAUDE.md / repo-rules content does not exist as a labeled section in notes.md; it would be inferable from §17.10 isolation + §17.4 heuristic library + general §17 commitments, not from 227-275. **Citation is wrong.** — severity high
- 03-phase-map.md:38 — observation: Cites `schema-stressors-v0.md:1-660` (P1.review) but the file is 557 lines (`wc -l` confirmed). End-line 660 overshoots by 103 lines. **Out-of-bounds citation.** — severity medium
- 03-phase-map.md:54, 04-parallelization-plan.md:103 — observation: Both cite `ops/legal-framing.md` Risk 7 for "container isolation policy" (P5.review) / "Slither sandbox" (Worker B2). legal-framing.md:76-84 Risk 7 is "Findings dispute and rebuttal" — has nothing to do with containers/sandboxing. Risk 10 (lines 104-110) is "AI-generated content liability" — also not container isolation. **legal-framing.md does not contain any container/Docker/cgroups content** (`grep -E "Docker|cgroups|container|sandbox|isolation"` returns 0 matches against legal-framing.md). The container-isolation policy is in notes.md §17.10. **Citation is wrong; the file does not contain the cited concept.** — severity high
- 00-identity.md:29 — observation: Cites `/root/Silica/design/multi-vm-svm-sketch.md:79` for "Anchor + solana-test-validator runner". Line 79 is mid-§"SVM-SYSVAR-SPOOFING" bug-class section. The actual Anchor + solana-test-validator content is at lines 113-122 (§"Fork environment" and §"PoC framework"). **Citation drifted by ~35 lines.** — severity medium
- 00-identity.md:30 — observation: Cites `/root/Silica/design/schema-draft-v0.md:115` for "Etherscan + Sourcify + multi-Solc compile". Line 115 is `"resource_types": ["Vault::Position", "Vault::AccessCap"]` inside the Move Locator example. No source-fetcher/Etherscan content at line 115. **Citation is wrong.** — severity high
- 00-identity.md:31 — observation: Cites `/root/Silica/design/heuristic-schema.md:188` for "Postgres schema for findings, heuristics, audits". Line 188 is "Don't auto-mint heuristics from `disputed` findings." inside §"Heuristic mining (auto-mint)". The Postgres schema is at line 205-207 (§"Storage"). **Citation drifted by ~17 lines.** — severity medium
- 03-phase-map.md:74, 04-parallelization-plan.md (P10 row) — observation: Cites `design/cost-model.md:121-140` for "budget enforcement"; lines 121-140 cover §"Sensitivity analysis" + §"Caching". Budget enforcement is at lines 111-119. **Citation is wrong section.** — severity medium
- 02-acceptance-criteria.md:119-129 — observation: Six-Gate CI commands look real (radon CLI flags `cc -a -nc`; jscpd; depcruise; npm audit; bandit). Spot-checked: radon `cc -a -nc` ("complexity, average, no-color") is valid. `npx depcruise --validate <config>` is valid. `python3 -m bandit -r tools/ -lll` is valid. ESLint / typescript / vitest commands are conventional. **No fabricated tool API found in §J.** — severity low (no defect)
- 01-use-case-frame.md:73-99 — observation: Finding TS interface (Contract A) cited from `schema-draft-v0.md:9-39`; line 9 is "VM-agnostic at its top level" goal #1, the actual interface is at lines 17-49. **Off by 8-10 lines but correct file/region.** — severity low
- 01-use-case-frame.md:104-145 — observation: Cites `schema-draft-v0.md:50-110` for per-VM Locators; valid range. Cites `schema-stressors-v0.md:541-580` for v0.1 off-chain extension; lines 541-547 cover the locked v0.1 decision items. The actual OffChainLocator example is at lines 350-365 (Stressor 7 BadgerDAO). The cite captures the decision narrative but not the schema variant; reasonable but imprecise. — severity low
- 04-parallelization-plan.md:31 — observation: Worker A1's tool_guidance instructs "search /root/nexus/src/contract/" for Zod-pattern style reference. 00-identity.md:67 says "/root/nexus/ — never modify (sister project)" — read is permitted. README.md:68 says "/root/nexus/, any sister repo — never touched" — stricter wording. The two are inconsistent on read access. — severity low
- 04-parallelization-plan.md:226, 07-verification-gates.md:5, 10-reporting-contract.md:39-49 — observation: bob:873-883 (Six-Layer Review Framework) citation lands within bob lines 873-883. Verified bob:878 is L1 Requirement fidelity, bob:879 is L2 Logic, bob:880 is L3 API integrity, bob:881 is L4 Security, bob:882 is L5+L6 (yes, both layers cite bob:882 and the source actually puts L5/L6 close together; bob:881 is L5 in the actual source). Inspecting bob:875-882 carefully: L5 actually maps to bob:880 ("Context awareness"), and the spec cites L5 as bob:882 — **off by one in the spec table**. Specifically: bob:880 is "L5 Context awareness", bob:881 is "L6 Test quality" — re-read of the bob source contradicts the spec's L3=bob:880, L4=bob:881, L5=bob:882, L6=bob:882 mapping. Let me re-verify: bob:878 = "L1 Requirement fidelity"; bob:879 = "L2 Logic & edge cases"; bob:880 = "L3 API integrity"; bob:881 = "L4 Security"; bob:880 also has "L5 Context awareness" duplicate? Reading the actual bob source more carefully shows the layers are bunched in a single table at bob:875-881 with each cell a row; the spec maps L5/L6 to bob:882 which is the *commentary line below the table*, not a layer line. **Spec maps L5/L6 to wrong bob lines.** — severity high

## Layer 4 — Security

- 00-identity.md:61-71, README.md:61-71 — observation: Do-not-touch list covers ~/.claude/CLAUDE.md, /root/CLAUDE.md, /root/.claude/skills/*/SKILL.md, /root/.claude/bob, /root/Silica/notes.md, /root/Silica/{design,ops,research}/, /root/nexus/, secrets. Does NOT explicitly list `/root/.claude/CLAUDE.md` (only `~/.claude/CLAUDE.md`); on this Linux host with `root` as the user, `~` resolves to `/root` so they're equivalent — but the spec mixes the two notations. Also does NOT explicitly forbid mutation of `/root/.claude/skills/author-agent-spec/scripts/*` even though §O acceptance criterion runs validate_spec.py from there. — severity low
- 09-stop-conditions.md:5-19 — observation: Hard-stop §1 lists CLAUDE.md mutation, skill-registry mutation, notes/design/research/ops mutation, sister-project writes. Adequately enforces the locked do-not-touch boundaries. — severity low (no defect)
- 01-use-case-frame.md:200, 07-verification-gates.md:12, 02-acceptance-criteria.md:135-141 — observation: Hostile-input-by-default invariant #2 is stated and tested at §K (test:security on src/llm/ and tools/). Per-layer L4 review checks "Prompt-injection from contract-source comments / NatSpec / strings". The test suite exists in spec text only — actual files do not yet exist (P20 ships them). The invariant is preserved across the spec but the **5-pattern injection corpus claim is a fabrication** (Layer 3 finding) — only 3 patterns are sourced. — severity medium
- 03-phase-map.md:106-107, 04-parallelization-plan.md:202-203, 09-stop-conditions.md:19, 02-acceptance-criteria.md:100 — observation: scope_artifact_id authorization is enforced at multiple stations: P17 D1 worker writes `src/scope/{artifact.ts,store.ts,enforce.ts}`; P18 D2 worker boundary requires "scope_artifact_id check is mandatory"; §H acceptance has `--scope-required-test`; 09 §1 hard-stops on "Off-chain agent runs without a valid scope_artifact_id". End-to-end enforcement appears intact. — severity low (no defect)
- 03-phase-map.md:16-18, 04-parallelization-plan.md, 01-use-case-frame.md:19, README.md:78 — observation: LLM trust-tier routing (notes.md §17.3) is documented at 01-use-case-frame.md:19 ("trust-tier routing") and 03-phase-map.md:16 (P7). However, **the trust-tier routing is not surfaced in P9 (3 baseline agents) or P15 (SVM agents) prompts** — the spec does not specify how an agent prompt declares its trust tier (Anthropic-no-retention vs. self-hosted vLLM) at invocation time. P7 builds the gateway; P9/P15 should specify per-agent tier selection. The hooks are missing. — severity medium
- 03-phase-map.md:12, 04-parallelization-plan.md:54-69, 09-stop-conditions.md:65 — observation: Three-pool isolation (Public / Shared / Private) is documented in the schema (P3) and acceptance §I checks pool counts but **the spec does not specify a P3 / P19 test that verifies a Private-pool finding cannot leak to Shared/Public without sanitization**. notes.md §17.4 + invariant 5 (01-use-case-frame.md:204) require sanitization on promotion; the test surface for the sanitization step is not pinned. — severity medium
- 04-parallelization-plan.md:103-114, 117-128, 191-218 — observation: Container isolation specifications appear in worker contracts: B2 ("4GB RAM, 2 CPU, 300s timeout, no network egress") and B3 ("Per-test wall-clock cap: 600s"). Group D (perimeter) workers do not include container limits in tool_guidance — D1 (frontend + RPC + scope) and D2 (subdomain + ci-secrets + multisig) write Dockerfiles per surface but no cgroups limits or network-egress restriction is specified at the worker contract level. notes.md §17.10 mandates these; the spec does not pin the policy at the per-worker contract. — severity medium
- 04-parallelization-plan.md:31, 06-context-handoff.md:55-63 — observation: §"Parallel-worker context isolation" (06 lines 55-63) restricts worker reads to "cited design doc(s)" and "source files of any dependencies cited in output_format". Worker A1's tool_guidance says "search /root/nexus/src/contract/" — `/root/nexus/` is not a Silica dependency cited in A1's output_format. **Worker A1 instruction violates the worker context-isolation rule.** — severity medium
- 03-phase-map.md, 04-parallelization-plan.md — observation: No worker contract has an instruction that could plausibly cause a CLAUDE.md mutation. The 9 §1 hard-stop list halts on any CLAUDE.md mutation. Boundary defense holds. — severity low (no defect)
- 04-parallelization-plan.md (Group D D2 worker) — observation: D2 includes "Multisig: on-chain Safe.getOwners(), ENS resolution, Etherscan name tags." This ships a tool that calls live Etherscan / RPC. The spec does not define an offline / mock mode for tests — running the test suite (especially in CI) would either need real Etherscan keys or a mock that returns structured fake data. The acceptance §H test (`tools/perimeter/{subdomain,ci-secrets,multisig}/`) does not specify mock vs. live. Risk: secret-handling / rate-limit drift across CI. — severity low

## Layer 5 — Context awareness

- 03-phase-map.md:28, 04-parallelization-plan.md:54-69, 06-context-handoff.md:25 — observation: P3 (worker A3) writes the heuristic store as `src/heuristic/store.ts` (flat). P19 test references `src/heuristic/{store,mining,drift}/` (subdir). 06 hands off P3 → P19 as "summary" (psql query + heuristics/baseline directory listing). Either the store is a file (P3) or a subdir (P19); spec does not reconcile. **Inconsistency between sections.** — severity medium
- 03-phase-map.md:21, 04-parallelization-plan.md:131-175 — observation: Group C splits P12 (5 EVM cases) and P13 (30 heuristics) into parallel workers. C1 must reference heuristic IDs from C2's seeded HEUR-* JSON files in `expected-finding.json`. Workers run concurrently. The §I acceptance gate counts "≥1 cited" findings — depends on C1+C2 coordinating IDs without seeing each others' files (worker context isolation). The handoff contract (06) does not address C1↔C2; only C → P20. **Coordination gap between C1 and C2.** — severity medium
- 05-skill-choreography.md:57-73, 03-phase-map.md, README.md:46-58 — observation: Canonical skill order at 05:57-73 is plan → name → clean-before-change → tests-first → codex → decide-duplicate → simplify → check-test-quality → review-ai-code. README executor walkthrough at line 46-58 lists the same skills in a different order: plan → name → clean-before-change → tests-first → codex → decide-duplicate. The two are not contradictory but README omits steps 7-9 from the canonical order. — severity low
- 03-phase-map.md:20 — observation: P11 (first end-to-end Euler audit) depends_on P4, P5, P6, P10. P10 depends_on P3, P8, P9. P8 depends_on P1, P2, P3, P5, P6, P7. So P11 transitively depends on P0–P10 — confirmed. All required upstream contracts are in place by the time P11 runs. — severity low (no defect)
- 06-context-handoff.md:7-26, 03-phase-map.md:21 — observation: 06 handoff matrix does NOT have a row for P12 → P19 nor for P13 → P19; P19 depends on P3, P13, P16 per 03-phase-map. The P3, P13, P16 → P19 row exists at line 25 ("summary" handoff), but P12 → anything (other than P20) is absent. P12 outputs (5 bench cases) are needed by P19 for drift-monitor regression coverage; the spec is silent on this handoff. — severity low
- 10-reporting-contract.md:121-126 — observation: Status board update behavior says "- [ ] **PN**" → "- [x] **PN**" only after BOTH .review AND .test boxes flip. 03-phase-map.md:33-115 has the parent-checkbox + 2 child-checkbox structure with Python parser at 119-133. Logic aligns. — severity low (no defect)
- 03-phase-map.md:114, 04-parallelization-plan.md:222-234 — observation: P20.review runs whole-spec `review-ai-code` six-layer review; per-phase reviewer template at 04:222-234 is also six-layer. P20.review verdict template requires "approve OR proceed-with-changes with ≤3 findings, none security or API integrity". Per-phase template (04:228-229) only emits the verdict literal. Verdict policies are consistent. — severity low (no defect)
- 04-parallelization-plan.md:236-243 — observation: Anti-fan-out justifications are reasonable (P0 single-author CLAUDE.md, P7-P10 strict chain, P11 single milestone, P14-P16 SVM chain, P19 single-author narrative, P20 final gate). No false-fan-out detected. — severity low (no defect)

## Layer 6 — Test quality

- tests/test_spec_structure.py:46-58 — observation: `test_phase_map_has_review_and_test_children_per_parent` is behavior-pinning — it asserts on the structural rule "every parent has a .review and .test child", which is the documented invariant. Strong test. — severity low (no defect)
- tests/test_spec_structure.py:61-72 — observation: `test_phase_table_dependencies_resolve` validates DAG resolution. Behavior-pinning. — severity low (no defect)
- tests/test_spec_structure.py:75-82 — observation: `test_phase_table_budgets_set` checks for empty / em-dash budgets. Tight; will catch unbounded phases. — severity low (no defect)
- tests/test_spec_structure.py:85-94 — observation: `test_worker_contracts_have_four_fields` asserts each yaml block has objective/output_format/tool_guidance/boundaries. Confirms structural completeness; does not verify the field bodies are non-empty or non-trivial. A worker contract with `objective: TODO` would pass this test. **Truthy-pass risk.** — severity medium
- tests/test_spec_structure.py:97-122 — observation: `test_acceptance_criteria_have_executable_predicates` checks for at least one EXEC_PATTERNS match in each §A–§O body. Skips §S (Summary table). The "exits 0" fallback pattern matches any prose containing "exits 0", so a §section that says "make it exit 0" without an actual command would still pass. **Permissive-truthy risk.** — severity medium
- tests/test_spec_structure.py:125-136 — observation: `test_no_manual_verification_phrases_in_gates` checks 5 forbidden phrases. Reasonable, but does not include "by hand", "operator confirms", "human verifies", which are common manual-verification leaks. **Edge-case gap.** — severity low
- tests/test_spec_structure.py:139-153 — observation: `test_bob_citations_in_verified_range` checks that bob:N citations land within the bob file's line count. This validates RANGE only, not that the cited content matches the framework name in the spec. As demonstrated in Layer 3, bob:882 is cited for both L5 and L6 in the spec but the bob source uses bob:882 for commentary, not layer definitions. **Test passes despite semantic-citation drift.** — severity high
- tests/test_spec_structure.py — observation: **No test validates `notes.md:N` citations** (or any non-bob external citation: design/*, ops/*, research/*, or skill SKILL.md ranges). Layer 3 documented 7 wrong notes.md citations and 4 wrong design/ops citations — none would be caught by the test suite. The drift gate at 07-verification-gates.md §D only validates bob citations. **Critical test-coverage gap.** — severity high
- tests/test_spec_structure.py:166-194 — observation: `test_required_artifact_top_level_dirs_owned_by_phase` checks artifact paths from 00-identity.md appear in phase-map / parallelization-plan. Skips brace-expanded entries — which means worker contracts using `{a,b,c}` shorthand for multiple files do not get verified. This is a **test edge-case where a missing path inside brace expansion is not caught**. — severity low
- tests/test_spec_structure.py:156-163 — observation: `test_phase_count_matches_locked_v1_scope` asserts exactly 21 phases (P0–P20). Strong / tight. — severity low (no defect)
- (negative space) — observation: No test verifies that **bob:NNN-NNN citations point at the framework named in the surrounding spec text**. E.g., a citation `bob:1-5 (Six-Layer Review Framework)` would pass `test_bob_citations_in_verified_range` because lines 1-5 exist, even though they discuss Executive Summary, not Six-Layer Review. This is the same class of failure as the wrong notes.md citations. — severity medium
- (negative space) — observation: No test asserts that every `*.review.md` template ends with a literal "verdict: <value>" line, despite 10-reporting-contract.md:48-50 making the verdict line load-bearing for parsers. A reviewer that emits a malformed verdict would be detected at runtime via "reviewer-malformed-output retry" (08-failure-recovery.md), but no spec-time test verifies the template wording itself. — severity low

## Summary

- L1: 1 critical, 0 high, 2 medium, 1 low → **4 findings**
- L2: 0 critical, 0 high, 4 medium, 3 low → **7 findings**
- L3: 0 critical, **8 high**, 5 medium, 4 low → **17 findings**
- L4: 0 critical, 0 high, 5 medium, 4 low → **9 findings**
- L5: 0 critical, 0 high, 2 medium, 5 low → **7 findings**
- L6: 0 critical, 2 high, 3 medium, 4 low → **9 findings**
- Total: **53 findings** (critical: 0; high: 10; medium: 19; low: 24)

## Verdict
verdict: block