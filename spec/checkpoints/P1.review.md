# Review of P1 — 2026-05-08T00:00:00Z

## Layer 1 — Requirement fidelity (bob:873-883)

Phase row: Finding schema (TS + Zod + canonical_id) per design/schema-draft-v0.md and schema-stressors-v0.md v0.1 extension.

Checking deliverables:
- `src/finding/schema.ts`: FindingSchema with all fields from Contract A (schema_version, id, canonical_id, audit_id, tenant_id, created_at, updated_at, subject, class, severity, confidence, validation, evidence, heuristics_cited, agent_provenance, remediation, composite_of, supersedes, related_to, scope_artifact_id, status, lifecycle). All present.
- Subject as discriminated union: evm | svm | off-chain — correct. off-chain requires scope_artifact_id — verified in tests.
- `src/finding/canonical.ts`: computeCanonicalId with RFC 8785 + 0x1f separator formula — matches notes.md §17.7.
- `src/finding/lifecycle.ts`: transitionFindingStatus, appendLifecycleEvent, isAllowedTransition — correct.
- CONFIDENCE_CEILING constants: all 12 rungs with values matching validation-tiers.md confidence ceiling table.

Spec invariants:
- Invariant #1 (VM-agnosticism): spine code imports only zod + node crypto. No EVM/SVM tool imports. PASS.
- Invariant #3 (confidence bounded by rung): CONFIDENCE_CEILING enforced; test coverage confirms all 12 rungs. PASS.

Finding: None. **Layer 1: PASS**

## Layer 2 — Logic & edge cases (bob:873-883)

- Empty evidence/heuristics_cited arrays: both default to `[]` in Zod schema — correct.
- Terminal lifecycle states (rejected, fixed): no outgoing transitions — test confirms this.
- off-chain subject without scope_artifact_id: test confirms rejection (Zod discriminated union requirement).
- Canonicalization: key-order independence tested (RFC 8785 determinism confirmed).
- Address normalization: EVM address lowercased in subjectToCanonicalLocator — prevents canonical_id drift from case differences.

Finding: None. **Layer 2: PASS**

## Layer 3 — API integrity (bob:873-883)

- `canonicalize` npm package: real package (RFC 8785 implementation), CJS format. `createRequire` fix is correct for ESM.
- Zod API: `z.discriminatedUnion('kind', [...])` is valid Zod v3 API. `z.number().int().nonnegative()` is valid.
- `node:crypto` createHash is stable Node.js built-in.
- ValidationRungName enum values match verbatim strings from validation-tiers.md.
- CONFIDENCE_CEILING values match validation-tiers.md table exactly (spot-checked: R0=0.60, R3=0.92, R9=0.95, R10=0.99).

Finding: None. **Layer 3: PASS**

## Layer 4 — Security (bob:873-883)

- No credentials in code.
- No imports from tools/, db/, or src/llm/ — domain isolation maintained.
- canonical.ts uses `sha256` — appropriate for a stable dedup key.
- Finding schema does not expose tenant_id through any leaked path.
- off-chain invariant: scope_artifact_id is required at the type level — cannot construct a valid off-chain Finding without it.

Finding: None. **Layer 4: PASS**

## Layer 5 — Context awareness (bob:873-883)

- No contradictions with P0 scaffolding.
- schema.ts correctly exports types re-used by validation/tiers.ts (ValidationRungName, CONFIDENCE_CEILING) — anticipating P2's import.
- Lifecycle terminal states (rejected, fixed) match Contract E (AuditJobState) pattern in spirit.

Finding: None. **Layer 5: PASS**

## Layer 6 — Test quality (bob:873-883)

- 33 tests across 3 files.
- Tests assert behavior (Zod parse result, canonical_id output, status after transition) — not call counts.
- Edge cases covered: invalid address, score > 1.0, invalid status enum, empty heuristics list, key-ordering stability.
- off-chain missing scope_artifact_id rejection is explicitly tested.
- No xfail/skip.

Finding: None. **Layer 6: PASS**

## Summary

| Layer | Status | Findings |
|-------|--------|----------|
| L1 | PASS | 0 |
| L2 | PASS | 0 |
| L3 | PASS | 0 |
| L4 | PASS | 0 |
| L5 | PASS | 0 |
| L6 | PASS | 0 |

## Verdict
verdict: approve
