# Review of P17 — 2026-05-08T00:00:00Z

## Layer 1 — Requirement fidelity (bob:873-883)

Phase: Frontend taint + RPC fingerprinter + scope artifact authorization.
- ScopeArtifact Zod schema: id, audit_id, tenant_id, targets, permitted_surfaces, depth, expires_at, authorized_signer. PASS.
- requireScope(): throws ScopeEnforcementError if no scope, expired, or surface not permitted. PASS.
- Frontend taint: ScopeMissing on null scope_artifact_id; BadgerDAO-pattern detection in mock mode. PASS.
- RPC fingerprinter: HIGH_RISK_METHODS list (personal_*, admin_*, debug_*, txpool_*); ScopeMissing without scope. PASS.

Finding: None. **Layer 1: PASS**

## Layer 2 — Logic & edge cases (bob:873-883)

- Expired scope artifact raises with 'expired' in message — tested.
- Surface not in permitted_surfaces raises with 'not permit' — tested.
- Empty string scope_artifact_id treated same as null — tested.
- Frontend mock analysis returns critical-severity finding — correct for BadgerDAO pattern.

Finding: None. **Layer 2: PASS**

## Layer 3 — API integrity (bob:873-883)

- Zod schema uses z.discriminatedUnion / z.enum correctly.
- RPC method names are exact Ethereum JSON-RPC method names.
- Scope permitted_surfaces enum matches OffChainLocator.off_chain_kind values in schema.ts. PASS.

Finding: None. **Layer 3: PASS**

## Layer 4 — Security (bob:873-883)

- All off-chain tools require scope_artifact_id — fail-closed confirmed by tests. PASS.
- Network egress: frontend allows target domain only (enforced at run.py invocation via ALLOW_LIST env). PASS.
- Container: 2GB RAM, 1 CPU, --security-opt no-new-privileges. PASS.
- No credentials in code.

Finding: None. **Layer 4: PASS**

## Layer 5 — Context awareness (bob:873-883)

- scope/artifact.ts has no imports outside Zod — domain-pure. PASS.
- P10.review noted scope_artifact_id enforcement deferred to P17 — delivered. PASS.

Finding: None. **Layer 5: PASS**

## Layer 6 — Test quality (bob:873-883)

- 17 tests across 3 files.
- Scope enforcement: null, empty string, expired, wrong surface — all edge cases covered.
- BadgerDAO mock detection explicitly tested — validates the §H acceptance criterion.

Finding: None. **Layer 6: PASS**

## Summary

| Layer | Status | Findings |
|-------|--------|----------|
| L1–L6 | PASS | 0 |

## Verdict
verdict: approve
