# Checkpoint P1 — 2026-05-08T00:00:00Z

## Phase row
| P1 | Finding schema (TS + Zod + canonical_id) | `plan-feature-architecture` → `code-with-tests-first` → `codex` | P0 | A | 80 calls | 120k |

## Inputs read
- `/root/Silica/design/schema-draft-v0.md:17-320` — Finding interface, Subject locators, Classification, Severity, Confidence, Validation, Evidence, HeuristicCitation, AgentProvenance, Remediation, Canonicalization, Lifecycle
- `/root/Silica/design/schema-stressors-v0.md:325-420` — Stressor 7 (BadgerDAO off-chain v0.1 extension)
- `/root/Silica/spec/01-use-case-frame.md:71-102` — Contracts A and B (verbatim Finding + Subject interfaces)
- `/root/Silica/notes.md:581-599` — §17.7 canonicalization formula (RFC 8785 + 0x1f separators)

## Artifacts produced
- `/root/Silica/src/finding/schema.ts` — 477 lines, sha256:f26d5ce2e57b191533165df7a0156472b53d3b08b5ad2ee83da4f8290d86f7db
- `/root/Silica/src/finding/canonical.ts` — 84 lines, sha256:3c9d4b1195b5755bda3e4fb42f3110b5b77c3147785f2a1f83f7f725f7d2bb4f
- `/root/Silica/src/finding/lifecycle.ts` — 74 lines, sha256:9176d44892e9333e8da65f159ef920a500c3d8562cf8dcf13ee1b3c81aed5d9f
- `/root/Silica/tests/unit/finding/{schema,canonical,lifecycle}.test.ts` — 33 tests

## Commands run
```bash
cd /root/Silica && npx vitest run --pool=forks --fileParallelism=false tests/unit/finding/
# Tests: 33 passed (3 files)
cd /root/Silica && npm run typecheck
# Exit 0, zero type errors (canonicalize CJS import resolved via createRequire)
```

## Gates passed
- C (TDD red-then-green): RED gate confirmed (tests written before implementation); GREEN: 33/33 pass
- B/G1 (coverage): unit tests cover schema validation, canonical_id computation, lifecycle transitions
- D (drift gate): validated before checkpoint write

## Findings
- Minor: `canonicalize` package exports CJS `module.exports`, not a named ESM export. Resolved with `createRequire` pattern. Documented in the file with a comment.

## Notes / surprises
- Zod's `.nonneg()` method does not exist on this version; replaced with `.nonnegative()`.
- The `canonicalize` package's default export in ESM context is the module namespace object, not the function. `createRequire` is the correct fix.
- off-chain `Subject` requires `scope_artifact_id` as a required field in the discriminated union (v0.1). Tests confirm it is rejected when missing.

## Verdict
complete
