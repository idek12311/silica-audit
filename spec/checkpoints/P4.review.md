# Review of P4 — 2026-05-08T00:00:00Z

## Layer 1 — Requirement fidelity (bob:873-883)

Phase: Source fetcher (Etherscan + Sourcify + multi-Solc compile + bytecode-equivalence).
- `fetch.py`: SourceResolution dataclass with source_files, compiler_version, compiler_settings, source_format, sourcify_verified, etherscan_verified, bytecode_match, toolchain_manifest. Covers all required fields.
- Resolution order: Sourcify first (open, no rate limit), then Etherscan fallback — correct per worker contract.
- `equivalence.py`: check_bytecode_equivalence strips CBOR metadata suffix before comparison — correct per notes.md §11.3.
- Dockerfile: multi-version solc via py-solcx — correct.

Finding: None. **Layer 1: PASS**

## Layer 2 — Logic & edge cases (bob:873-883)

- Multi-file Etherscan format (double-brace JSON) handled.
- Sourcify 404 → fallback to Etherscan tested.
- Missing ETHERSCAN_API_KEY raises with clear message.
- CBOR metadata stripping: handles edge case where cbor_len >= bytecode length.
- Empty source files: returns from both providers handled.

Finding: None. **Layer 2: PASS**

## Layer 3 — API integrity (bob:873-883)

- Etherscan API: module=contract, action=getsourcecode — correct Etherscan v2 API.
- Sourcify API: /files/{match_type}/{chain_id}/{address} — correct Sourcify v1 API format.
- eth_getCode RPC method — correct Ethereum JSON-RPC.
- ETHERSCAN_CHAIN_API dict covers chain IDs 1, 137, 42161, 10, 56, 43114 — correct addresses.

Finding: None. **Layer 3: PASS**

## Layer 4 — Security (bob:873-883)

- No API keys hardcoded; api_key from parameter or env ETHERSCAN_API_KEY.
- Dockerfile runs as non-privileged user (python:3.11-slim default).
- Docker run.py uses --network none for static-analysis context (equivalence needs network — noted).
- No secret leaks in code.

Finding: None. **Layer 4: PASS**

## Layer 5 — Context awareness (bob:873-883)

- fetch.py and equivalence.py are pure Python with requests only — no Silica TS imports.
- SourceResolution.toolchain_manifest shape matches design/schema-draft-v0.md ToolchainManifest.

Finding: None. **Layer 5: PASS**

## Layer 6 — Test quality (bob:873-883)

- 7 tests: parsing, fallback behavior, error cases, build manifest.
- Tests use mock patches on requests.get — avoids real network calls.
- Missing test: multi-file JSON source with >2 files — minor.

Finding (L1 — minor): No test for Sourcify full_match vs partial_match priority. Low risk.

## Summary

| Layer | Status | Findings |
|-------|--------|----------|
| L1 | PASS | 0 |
| L2 | PASS | 0 |
| L3 | PASS | 0 |
| L4 | PASS | 0 |
| L5 | PASS | 0 |
| L6 | PASS — minor | 1 (low) |

Total: 1 minor finding.

## Verdict
verdict: proceed-with-changes
