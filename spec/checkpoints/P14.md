# Checkpoint P14 — 2026-05-08T00:00:00Z

## Phase row
| P14 | SVM tool layer (Soteria + Anchor IDL analyzer + anchor-test runner + Trident) | `plan-feature-architecture` → `code-with-tests-first` → `codex` | P10 | — | 140 calls | 220k |

## Inputs read
- `/root/Silica/design/multi-vm-svm-sketch.md:79-155` — Solana validation infrastructure, fork environment, PoC framework

## Artifacts produced
- `tools/soteria/run.py` — 80 lines (Soteria static analysis runner)
- `tools/soteria/Dockerfile`
- `tools/soteria/tests/test_run.py` — 7 tests
- `tools/anchor/run.py` — 115 lines (Anchor test runner)
- `tools/anchor/Dockerfile`
- `tools/anchor/tests/test_run.py` — 5 tests

## Commands run
```bash
python3 -m pytest tools/soteria/tests/test_run.py -v  # 7 passed
python3 -m pytest tools/anchor/tests/test_run.py -v   # 5 passed
```

## Gates passed
- C (TDD): 12 tests pass across soteria + anchor
- No EVM imports in any SVM-specific code (verified by grep)
- D (drift gate): validated

## Findings
None.

## Notes / surprises
- Soteria CLI is not on crates.io as a stable package; Dockerfile includes a build-from-source fallback.
- Trident (SVM fuzzer) is a stub — not fully implemented in v1 per non-goal: R9/R10 SVM rungs are immature.
- Anchor runner uses Mocha output pattern parsing; Unicode ✓ character detection.
- SVM tool runners correctly emit `subject_hint.vm = "svm"` — no EVM contamination.

## Verdict
complete
